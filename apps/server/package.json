{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "test": "bunx vitest run --config vitest.config.ts"
  },
  "dependencies": {
    "@generai/auth": "workspace:*",
    "@generai/config": "workspace:*",
    "@generai/db": "workspace:*",
    "@generai/env": "workspace:*",
    "@generai/mail": "workspace:*",
    "@hono/zod-openapi": "^1.6.0",
    "@langchain/core": "^1.2.8",
    "@langchain/openai": "^1.5.8",
    "@scalar/hono-api-reference": "^0.11.14",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "hono": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@types/node": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "@vitest/ui": "catalog:",
    "tsdown": "^0.22.14",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
