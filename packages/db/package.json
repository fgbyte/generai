{
  "name": "@generai/db",
  "type": "module",
  "exports": {
    ".": {
      "default": "./src/index.ts"
    },
    "./*": {
      "default": "./src/*.ts"
    }
  },
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:studio": "drizzle-kit studio",
    "db:migrate": "bun run src/migrate.ts",
    "db:migrate-staging": "bun run src/migrate.ts --staging"
  },
  "dependencies": {
    "@generai/env": "workspace:*",
    "@neondatabase/serverless": "^1.1.0",
    "dotenv": "catalog:",
    "drizzle-orm": "^0.45.2",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@generai/config": "workspace:*",
    "@types/ws": "^8.18.1",
    "drizzle-kit": "^0.31.10",
    "typescript": "catalog:",
    "ws": "^8.21.0"
  }
}
